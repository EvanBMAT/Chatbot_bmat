class centralSystem:
    def __init__(self,auth,fastDB,db,config):
        self.auth = auth
        self.fastDB = fastDB
        self.db = db
        self.config = config

    def checkAuth():
        if auth.
